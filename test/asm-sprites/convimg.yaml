palettes:
  - name: mypalette
    images: automatic

converts:
  - name: myimages
    palette: mypalette
    images:
      - oiram.png
      - thwomp.png

outputs:
  - type: asm
    include-file: gfx.inc
    palettes:
      - mypalette
    converts:
      - myimages

