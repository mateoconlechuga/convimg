palettes:
  - name: mypalette
    images: automatic

converts:
  - name: myimages
    palette: mypalette
    images:
      - ./inputs/*

outputs:
  - type: c
    directory: outputs
    palettes:
      - mypalette
    converts:
      - myimages

