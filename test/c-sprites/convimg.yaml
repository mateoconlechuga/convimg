palettes:
  - name: mypalette
    images: automatic

converts:
  - name: myimages
    palette: mypalette
    images:
      - oiram.png
      - thwomp.png

outputs:
  - type: c
    include-file: gfx.txt
    palettes:
      - mypalette
    converts:
      - myimages

