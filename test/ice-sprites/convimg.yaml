palettes:
  - name: mypalette
    images: automatic

converts:
  - name: myimages
    palette: mypalette
    images:
      - oiram.png
      - thwomp.png

outputs:
  - type: ice
    include-file: ice.txt
    palettes:
      - mypalette
    converts:
      - myimages

